{
  "name": "prime_checker_two",
  "version": "1.0.0",
  "description": "Creating a simple app that checks if a number is prime or not. It will be expanded to do other things with those numbers.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.4"
  }
}
